name: Update Filters
on:
  push:
    paths-ignore:
      - 'README.md'
      - '.github/**'
      - 'LICENSE'
      - 'rules/**'
    branches: [ main ]
  schedule:
    - cron: "19 17 * * *"
  workflow_dispatch:

env:
  TZ: Asia/Shanghai

jobs:
  Update_Filters:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    
    steps:
      # 第一步：拉取代码，fetch-depth: 0 确保获取完整历史以减少冲突
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 第二步：运行构建脚本
      - name: Built Rules
        run: |
          bash ./script/build-combined-rules.sh
      
      # 第三步：提交并强制推送
      # 这里添加了 push_options: '--force'，解决你遇到的 non-fast-forward 错误
      - name: Commit Changes
        id: commit
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 🚀 CI Updated
          push_options: '--force'

      # 第四步：清理旧的任务记录，保持 Action 界面整洁
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 1
